conda create -p venv1 python=3.10 -y
conda activate venv1/
pip install -r requirements.txt

git init
echo "MLOPS Assignment" >> README.md 
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/learn4fun24x7/MLOPS_Assignment.git
git push -u origin main

dvc init
dvc add data/raw/housing.csv

git add data/raw/.gitignore data/raw/housing.csv.dvc           
git commit -m "Add raw dataset with DVC tracking"
git push -u origin main

python ./src/preprocess.py

dvc add data/processed/X_train.csv
dvc add data/processed/X_test.csv
dvc add data/processed/y_train.csv
dvc add data/processed/y_test.csv

git add data\processed\.gitignore data/processed/*.dvc
git commit -m "Add processed data with DVC tracking"
git push -u origin main

mlflow ui
python ./src/train_register.py

uvicorn app.main:app --reload

{
  "longitude": 122.23,
  "latitude": 37.88,
  "housing_median_age": 41,
  "total_rooms": 880,
  "total_bedrooms": 129,
  "population": 322,
  "households": 126,
  "median_income": 8.3252
}

https://www.docker.com/products/docker-desktop/

docker build -t california-housing-api .
docker run -d -p 8000:8000 california-housing-api
